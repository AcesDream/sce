server:
  port: 9400

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: turbine-rabbitmq
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
